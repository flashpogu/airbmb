{
  "name": "airbnb",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd api && npm install) || (cd client && npm install)",
    "server": "node ./api/index.js",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
